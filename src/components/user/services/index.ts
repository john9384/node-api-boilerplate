import { ICreate } from '../types/dtos'
import { IUser } from '../types/model'

export const createUser = async (data: ICreate): Promise<IUser> => {
	// Check if user exists
	const newUser = {}
	return {}
}
